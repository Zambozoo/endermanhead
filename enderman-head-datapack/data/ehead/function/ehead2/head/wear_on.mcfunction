# runs as a player on the tick they put on an enderman head. apply the enderman-head vision: a custom
# invert post effect that also draws the crosshair eye. start on the closed-eye variant (unseen); the
# eye pass swaps to the open-eye variant while the wearer is seen.
posteffect add @s ehead:eye_closed
tag @s add ehead2.wearing_head
